name: Publish Java Client

on:
  push:
    paths:
      - 'java-client/**'
      - 'scripts/**'
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download OpenAPI Spec
        run: |
          curl -o openapi.yml https://api.smsh.sh/docs/openapi
          echo "Downloaded OpenAPI spec from production API"

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'corretto'

      - name: Setup Node.js (for OpenAPI Generator CLI)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install OpenAPI Generator CLI
        run: npm install -g @openapitools/openapi-generator-cli

      - name: Extract API Version
        id: version
        run: |
          API_VERSION=$(grep -oP 'version:\s*\K[^#\n]*' openapi.yml | tr -d ' "'"'")
          echo "version=$API_VERSION" >> $GITHUB_OUTPUT
          echo "Extracted API version: $API_VERSION"

      - name: Generate Java Client
        run: |
          VERSION="${{ steps.version.outputs.version }}"

          openapi-generator-cli generate \
            -i openapi.yml \
            -g java \
            -o java-client \
            --library webclient \
            --api-package sh.smsh.client.api \
            --model-package sh.smsh.client.model \
            --invoker-package sh.smsh.client \
            --group-id sh.smsh \
            --artifact-id reborn-api \
            --artifact-version "$VERSION" \
            --additional-properties=useRuntimeException=true,hideGenerationTimestamp=true,supportUrlQuery=true

      - name: Patch generated POM with publishing config
        run: |
          python3 scripts/patch-pom.py \
            java-client/pom.xml \
            "Reborn API Client" \
            "Java client for Reborn API"

      - name: Import GPG key
        run: |
          echo "${{ secrets.GPG_PRIVATE_KEY }}" | base64 -d | gpg --batch --import
          gpg --list-secret-keys --keyid-format LONG

      - name: Create Maven settings.xml
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml << 'EOF'
          <settings>
            <servers>
              <server>
                <id>central</id>
                <username>${env.MVN_CENTRAL_USERNAME}</username>
                <password>${env.MVN_CENTRAL_PASSWORD}</password>
              </server>
            </servers>
          </settings>
          EOF

      - name: Build and Publish to Maven Central
        working-directory: ./java-client
        env:
          MVN_CENTRAL_USERNAME: ${{ secrets.MVN_CENTRAL_USERNAME }}
          MVN_CENTRAL_PASSWORD: ${{ secrets.MVN_CENTRAL_PASSWORD }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
        run: |
          mvn clean deploy -Dgpg.keyname=${{ secrets.GPG_KEY_ID }}
